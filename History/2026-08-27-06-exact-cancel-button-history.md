# 작성 중 글 안내창의 정확한 취소 버튼 선택

## 발견된 문제

화면 전체에서 `취소`가 포함된 버튼을 찾을 때 스마트에디터 도구의 `취소선` 버튼이 먼저 선택됨.

## 수정

- `작성 중인 글이 있습니다` 문구가 포함된 확인 안내창으로 범위 제한
- 해당 안내창 내부의 버튼 중 텍스트가 정확히 `취소`인 버튼만 선택
- `취소선`처럼 일부 문자열만 일치하는 버튼은 제외

## 안전 상태

- 실패한 실행에서 제목·본문 입력 없음
- 임시저장 및 발행 없음
