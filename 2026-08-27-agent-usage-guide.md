# 정보성 블로그 제목·자료 수집 에이전트 사용 안내

## 실행 환경

이 에이전트는 현재 프로젝트 안의 Codex 작업에서 사용한다. 별도의 OpenAI API 키, 외부 유료 검색 API 또는 추가 Python 패키지가 필요하지 않다.

프로젝트 밖의 전역 스킬 폴더에는 설치하지 않았으므로, 현재 프로젝트에서 다음 스킬 파일을 명시해 실행한다.

`2026-08-27-information-blog-research-agent/SKILL.md`

## 기본 요청 예시

다음처럼 요청한다.

```text
프로젝트의 2026-08-27-information-blog-research-agent/SKILL.md 규칙으로 아래 제목을 분석하고 자료를 수집해줘.

1. 자동차보험 치료비는 누가 부담할까요?
2. 교통사고 후 병원은 언제 가야 할까요?
3. 운동 후 허리가 아픈 이유는 무엇일까요?
```

에이전트는 제목을 수정하지 않고 입력 순서대로 조사한다. 조사만 요청하면 각 제목의 정상 종료 상태는 `자료 확인`이다.

## 선택 작업

원고가 필요하면 자료 확인 결과를 받은 다음 별도로 요청한다.

```text
제목 2번만 확인된 자료 범위 안에서 정보성 원고로 작성해줘.
```

네이버 입력이 필요하면 대상 제목과 작업 범위를 명시한다. 의료 관련 글은 편집기 입력과 임시저장까지만 가능하며 자동 발행하지 않는다.

```text
검수가 끝난 제목 2번 원고를 네이버 편집기에 입력하고 임시저장까지만 진행해줘.
```

## 결과 파일

파일 저장을 요청하면 프로젝트 내부에만 새 파일을 만든다.

- 제목별 조사 보고서: `YYYY-MM-DD-번호-설명적이름.md`
- 배치 검증 장부: `YYYY-MM-DD-research-ledger.json`
- 작업 이력: `History/YYYY-MM-DD-research-history.md`

같은 이름의 파일이 있으면 덮어쓰지 않고 새 순번을 붙인다.

## 검증 명령

Python 3가 있으면 별도 설치 없이 내장 검증을 실행할 수 있다.

```powershell
python .\2026-08-27-information-blog-research-agent\scripts\2026-08-27-validate-research-output.py --self-test
```

실제 JSON 장부와 원본 제목을 함께 검사하는 예시는 다음과 같다.

```powershell
python .\2026-08-27-information-blog-research-agent\scripts\2026-08-27-validate-research-output.py `
  .\2026-08-27-research-ledger.json `
  --expected-title "자동차보험 치료비는 누가 부담할까요?" `
  --expected-title "교통사고 후 병원은 언제 가야 할까요?"
```

검증기는 제목 수, 순서, 원문, 필수 필드, 상태, 의료 안전 모드, 출처 구조, 유사 제목 표시를 확인한다. 출처 내용 자체의 진실성은 실행 시 웹 원문을 열어 별도로 판단해야 한다.

## 안전 경계

- 제목 수정이나 키워드 추가는 사용자 승인 후에만 한다.
- 확인되지 않은 수치, 사례, 통계, 치료 효과를 만들지 않는다.
- 의료법과 의료광고 기준은 원고 작성 시점의 공식 자료를 다시 확인한다.
- 의료 관련 글은 발행 또는 예약 발행하지 않는다.
- 기존 파일을 삭제하거나 덮어쓰지 않는다.
