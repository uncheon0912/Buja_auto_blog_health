# 의료법·금칙어·네이버 서식 검수 에이전트 사용 안내

## 실행 환경

이 에이전트는 현재 프로젝트의 Codex 작업에서 사용한다. 별도 API 키, 외부 유료 검색 API 또는 추가 Python 패키지가 필요하지 않다.

다음 스킬 파일을 명시해 실행한다.

`2026-08-27-medical-naver-content-review-agent/SKILL.md`

## 기본 검수 요청

```text
프로젝트의 2026-08-27-medical-naver-content-review-agent/SKILL.md 규칙으로 아래 원고를 네이버 입력 전에 검수해줘.

원본 제목: 운동 후 허리가 아픈 이유는 무엇일까요?
원고: ...
관련 조사 자료: ...
```

파일로 저장된 여러 원고를 검사할 때는 대상 파일과 원본 제목을 입력 순서대로 지정한다. 각 원고는 서로 독립적으로 검수된다.

## 결과 해석

글마다 다음 중 하나의 종합 결과가 표시된다.

- `통과`
- `자동 수정 후 통과`
- `사용자 확인 필요`
- `근거 부족`
- `의료 표현 재작성 필요`
- `네이버 서식 오류`

`통과` 또는 `자동 수정 후 통과`만 네이버 입력 가능으로 표시된다. 나머지 결과는 수정·근거 확인·사용자 판단 후 다시 검수해야 한다.

자동 수정안이 있으면 다음 표가 제공된다.

| 수정 전 문장 | 문제 유형 | 수정한 문장 | 수정 이유 |
|---|---|---|---|

검수 요청만으로 기존 원고 파일을 수정하지 않는다. 수정본 저장이 필요하면 검수 결과를 확인한 뒤 별도로 승인한다.

## 최신 법령 확인

의료법, 의료광고 또는 사전심의 판단이 필요한 경우 현재 국가법령정보센터와 보건복지부의 공식 원문을 다시 확인한다. 자동 검수 결과는 법률 자문이나 의료광고 사전심의 결과가 아니다.

## 검사기 실행

내장 예시 검사는 Python 3만 있으면 실행할 수 있다.

```powershell
$env:PYTHONUTF8='1'
python -B .\2026-08-27-medical-naver-content-review-agent\scripts\2026-08-27-review-content-draft.py --self-test
```

실제 원고 파일을 검사하는 예시는 다음과 같다.

```powershell
$env:PYTHONUTF8='1'
python -B .\2026-08-27-medical-naver-content-review-agent\scripts\2026-08-27-review-content-draft.py `
  .\검수할-원고.md `
  --title "운동 후 허리가 아픈 이유는 무엇일까요?"
```

JSON 결과가 필요하면 `--json`을 추가한다.

검사기는 다음 항목의 1차 의심 지점을 찾는다.

- 제목, 구분선, 고정 안내문, 제목 박스
- 인용구와 FAQ 박스
- 볼드와 형광펜 중첩·과다 사용
- 정보표의 열·행·빈 셀
- 수치·연구·순위 주장
- 지정된 의료 금칙어
- Q&A 개수와 위치

표현의 실제 의미, 출처의 적합성, 최신 법령 적용 여부는 에이전트가 공식 원문과 문맥을 별도로 검수해야 한다.

## 네이버 입력 제한

- 검수를 통과하지 못한 원고는 네이버에 입력하지 않는다.
- 콘텐츠 수정본 저장 승인과 네이버 입력 승인은 서로 별개다.
- 네이버 입력은 사용자가 별도로 요청한 경우에만 수행한다.
- 의료 관련 원고는 검수를 통과해도 임시저장까지만 처리한다.
- 발행, 예약 발행, 공개 전환은 수행하지 않는다.

## 기록

검수 결과와 수정 내역은 프로젝트 내부 `History` 폴더에 새 파일로 기록한다. 기존 원고와 기존 이력 파일을 덮어쓰지 않는다.
