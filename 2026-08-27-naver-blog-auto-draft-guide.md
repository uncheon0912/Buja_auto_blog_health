# API 없는 네이버 블로그 자동 임시저장 안내

## 목적

사용자가 Codex 대화에 `블로그 번호 + 제목`을 입력하면 Codex가 참고자료에 맞는 원고를 작성·검수하고, 이 로컬 프로그램이 해당 블로그의 새 글 화면에 제목과 본문을 입력한 뒤 임시저장합니다.

OpenAI API 키는 사용하지 않습니다. 네이버 아이디, 비밀번호, 인증 코드도 프로그램이나 History에 기록하지 않습니다.

## 최초 로그인

블로그 번호마다 최초 한 번 전용 Chrome 창에서 직접 로그인합니다.

```powershell
python .\2026-08-27-naver-blog-auto-draft.py setup --blog-number 1
```

로그인 상태는 `2026-08-27-blog-sessions/blog-01`처럼 블로그 번호별 Chrome 프로필에 보관됩니다. 이 폴더에는 로그인 세션이 포함될 수 있으므로 다른 사람에게 공유하거나 클라우드에 업로드하지 마세요.

## 임시저장 실행

검수된 제목 파일과 본문 파일은 프로젝트 내부에 있어야 합니다.

```powershell
python .\2026-08-27-naver-blog-auto-draft.py draft `
  --blog-number 1 `
  --blog-url "https://blog.naver.com/블로그아이디" `
  --title-file ".\Contents\2026-08-27-01-title.txt" `
  --body-file ".\Contents\2026-08-27-01-body.txt" `
  --save-draft
```

`--save-draft`가 없으면 저장 동작을 실행하지 않습니다. 프로그램에는 발행 기능이 없습니다.

## 안전 중단 조건

- 로그인, 2차 인증 또는 캡차가 나타남
- 제목이나 본문에 기존 내용이 있음
- 새 글쓰기 화면을 찾을 수 없음
- 임시저장 버튼 또는 저장 성공 상태가 불명확함
- 제목·본문 파일이 현재 프로젝트 밖에 있음

오류가 발생하면 가능한 경우 `History`에 날짜가 붙은 화면 이미지를 남기고, 같은 저장 동작을 자동으로 반복하지 않습니다.

## 현재 제한

첫 버전은 새 글 열기, 제목·본문 입력, 임시저장의 안전한 동작부터 검증합니다. 네이버 스마트에디터의 표·인용구·형광펜 등 실제 서식은 계정별 편집기 화면에서 첫 테스트를 확인한 뒤 별도 단계로 보강합니다.
