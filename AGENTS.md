# Codex 네이버 블로그 자동화 지침

## 제목 입력 감지

사용자가 `제목:`, `제목 :` 또는 문맥상 명확한 블로그 제목을 입력하면 다음 절차를 시작한다.

## Chrome 및 로그인 절차

1. 사용자의 Google Chrome을 제어할 수 있는 ChatGPT Chrome 확장프로그램 연결 상태를 확인한다.
2. 확장프로그램이 없거나 연결되지 않았으면 `README.md`의 설치 절차를 안내하고 작업을 중단한다.
3. Chrome으로 네이버 블로그 로그인 화면을 연다.
4. 반드시 다음 내용을 사용자에게 공지한다.

   `Chrome에서 네이버 로그인을 직접 완료해 주세요. 아이디, 비밀번호, 인증번호는 Codex가 입력하거나 저장하지 않습니다. 로그인이 끝나면 이 대화창에 "로그인 완료"라고 입력해 주세요.`

5. 사용자가 현재 대화에서 `로그인 완료`라고 명시하기 전에는 새 글 작성, 제목 입력, 본문 입력 또는 임시저장을 시작하지 않는다.
6. 로그인 입력란에 값을 입력하거나 로그인 정보, 쿠키, 인증번호를 읽거나 출력하거나 History에 기록하지 않는다.

## 글 작성 및 임시저장

1. `로그인 완료`를 받은 뒤 네이버 블로그의 새 글쓰기 화면으로 이동한다.
2. `작성 중인 글이 있습니다` 안내창이 나타나면 해당 안내창 안에서 텍스트가 정확히 `취소`인 버튼만 누른다.
3. 취소 후 안내창이 사라진 것을 확인하고 빈 새 글에서 시작한다.
4. 제목과 본문을 작성·검수한 뒤 네이버의 `저장` 버튼만 한 번 누른다.
5. 임시저장 완료 표시 또는 임시저장 글 수 증가를 확인한다.
6. `발행` 버튼은 절대 누르지 않는다. 공개 발행은 별도의 사용자 요청과 재확인 없이는 수행하지 않는다.
7. 저장 결과가 불명확하면 저장을 반복하지 말고 사용자에게 상태를 알린다.

## 로컬 정보 보호

- `2026-08-27-blog-sessions`는 브라우저 로그인 세션이 포함될 수 있는 로컬 전용 폴더다.
- 로그인 세션, 쿠키, 인증 정보, 오류 화면의 개인정보는 Git에 추가하지 않는다.
- 작업 결과에는 임시저장 여부와 검증 근거만 기록하고 인증값은 기록하지 않는다.
