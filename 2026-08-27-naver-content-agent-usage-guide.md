# 네이버 정보성 블로그 콘텐츠 생성 에이전트 사용 안내

## 실행 환경

이 에이전트는 현재 프로젝트의 Codex 작업에서 사용한다. 별도의 API 키, 외부 유료 검색 API 또는 추가 Python 패키지가 필요하지 않다.

프로젝트 밖의 전역 스킬 폴더에는 설치하지 않았으므로 현재 프로젝트에서 다음 스킬 파일을 명시해 실행한다.

`2026-08-27-naver-information-blog-content-agent/SKILL.md`

## 원고 생성 요청

```text
프로젝트의 2026-08-27-naver-information-blog-content-agent/SKILL.md 규칙으로 아래 제목마다 독립된 네이버 정보성 블로그 원고를 작성해줘.

1. 자동차보험 치료비는 누가 부담할까요?
2. 교통사고 후 병원은 언제 가야 할까요?
3. 운동 후 허리가 아픈 이유는 무엇일까요?
```

에이전트는 기존 자료수집 스킬의 조사 결과를 우선 확인하고, 필요한 최신 자료를 공식 원문에서 다시 검증한다.

## 승인과 저장

처음 생성된 원고는 채팅에서 전체 내용을 보여주기만 하며 파일로 저장하지 않는다. 제목별 예상 글자 수, 근거 확인 상태, 의료 표현 검수 결과도 함께 표시한다.

전체 원고가 적절하면 다음처럼 승인한다.

```text
전체 원고 승인. 콘텐츠 파일로 저장해줘.
```

일부 원고만 승인할 수도 있다.

```text
제목 1번과 3번 원고만 승인. 두 원고만 저장해줘.
```

승인된 원고는 프로젝트 내부 `Contents` 폴더에 `YYYY-MM-DD-번호-설명적이름.md` 형식의 새 파일로 저장된다. 같은 이름이 있으면 덮어쓰지 않고 새 순번을 붙인다.

콘텐츠 저장 승인은 네이버 입력 승인이 아니다.

## 네이버 입력과 임시저장

네이버 입력은 저장 승인과 별도로 요청한다.

```text
승인된 제목 3번 원고를 네이버 편집기에 입력하고 임시저장까지만 진행해줘.
```

의료 관련 원고는 임시저장까지만 처리한다. 발행, 예약 발행, 공개 전환은 수행하지 않는다.

## 초안 검증

Python 3가 있으면 별도 설치 없이 구조 검사를 실행할 수 있다.

```powershell
python -B .\2026-08-27-naver-information-blog-content-agent\scripts\2026-08-27-validate-content-draft.py --self-test
```

승인 전 실제 초안 파일을 별도로 전달받아 검사할 때는 제목 원문을 함께 지정한다.

```powershell
python -B .\2026-08-27-naver-information-blog-content-agent\scripts\2026-08-27-validate-content-draft.py `
  .\검토할-초안.md `
  --title "운동 후 허리가 아픈 이유는 무엇일까요?"
```

검증기는 다음 항목을 확인한다.

- 상단 제목과 제목 박스의 원문 일치
- 구분선 바로 다음의 고정 안내문
- 세 개의 인용구 소제목
- 형광펜 개수와 허용 색상
- 2열 표와 최대 네 개 데이터 행
- 2~3개의 볼드 Q&A 질문
- FAQ와 최종 정리 위치
- 필수 블록 순서
- 예상 글자 수

검증기는 구조를 확인하는 도구이며 의료·법률 내용의 정확성을 대신하지 않는다. 실제 원고는 작성 시점의 최신 공식 자료와 의료 안전 규칙으로 별도 검수한다.

## 파일 안전 규칙

- 기존 콘텐츠, 스킬 또는 이력 파일을 덮어쓰지 않는다.
- 승인받지 않은 원고를 콘텐츠 파일로 저장하지 않는다.
- 프로젝트 폴더 밖에는 파일을 만들거나 수정하지 않는다.
- 작업 결과와 사용자 결정은 `History`에 새 파일로 기록한다.
